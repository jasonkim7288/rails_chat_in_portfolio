<%= form_with(model: project, local: true) do |form| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :project_title %>
  </div>

  <div class="field">
    <%= form.label :image_addr %>
    <%= form.text_field :image_addr, id: :project_image_addr %>
  </div>

  <div class="field">
    <%= form.label :language %>
    <%= form.text_field :language, id: :project_language %>
  </div>

  <div class="field">
    <%= form.label :paragraphs %>
    <%= form.text_field :paragraphs, id: :project_paragraphs %>
  </div>

  <div class="field">
    <%= form.label :git_addr %>
    <%= form.text_field :git_addr, id: :project_git_addr %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
